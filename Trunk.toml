[build]
target = "index.html"
dist = "dist"

# Trunk injects a content-hashed inline loader, so generate its CSP hash after bundling.
[[hooks]]
stage = "post_build"
command = "cargo"
command_arguments = ["run", "--quiet", "--locked", "--package", "security-headers"]
